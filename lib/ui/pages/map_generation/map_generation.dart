import 'package:flutter/material.dart';

class MapGenerationPage extends StatelessWidget {
  const MapGenerationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
